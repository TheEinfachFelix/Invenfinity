<Page x:Class="InvenfinityApp.Views.Grid"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:InvenfinityApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:dto="clr-namespace:Backend.Application.DTOs;assembly=Backend"
      Title="Grid">

    <Grid>
        <TreeView ItemsSource="{Binding RootItems}">

            <TreeView.Resources>

                <!-- Template für Locations (Dropdown-Knoten) -->
                <HierarchicalDataTemplate DataType="{x:Type dto:DTOLocation}"
                                  ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"
                       FontWeight="Bold"/>
                </HierarchicalDataTemplate>

                <!-- Template für Grids (auswählbar) -->
                <DataTemplate DataType="{x:Type dto:DTOGrid}">
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>

            </TreeView.Resources>

        </TreeView>
    </Grid>
</Page>
