namespace InvenfinityApp.ViewModel
{
    public class MainViewModel
    {
        // Minimal placeholder ViewModel to satisfy compilation after SDK conversion.
        // Extend with actual properties/commands as needed.
    }
}
