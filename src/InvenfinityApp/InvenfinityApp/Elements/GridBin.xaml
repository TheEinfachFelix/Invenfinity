<UserControl x:Class="InvenfinityApp.Elements.GridBin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InvenfinityApp.Elements"
             xmlns:dto="clr-namespace:Backend.Application.DTOs;assembly=Backend"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="White"
        BorderThickness="2"
        Background="DarkSlateGray"
        CornerRadius="4"
        Padding="4"
        AllowDrop="True"
        PreviewMouseLeftButtonDown="Bin_MouseDown"
        PreviewMouseMove="Bin_MouseMove">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                    Text="Bin"
                    FontWeight="Bold"
                    Foreground="White"
                    Margin="0,0,0,5"/>

                <Button Content="Edit" 
                        HorizontalAlignment="Center" 
                        Grid.Column="1"
                        Click="Edit_Click"
                        Width="100"/>

                <DockPanel Grid.Column="2" HorizontalAlignment="Right">
                    <TextBlock Text="id: " FontWeight="Bold"
                        Foreground="White"
                        Margin="0,0,10,10" />
                    <TextBlock Text="{Binding BinId}" FontWeight="Bold"
                        Foreground="White"
                        Margin="0,0,10,10"/>
                </DockPanel>
            </Grid>

            <!-- PARTS -->
            <ItemsControl ItemsSource="{Binding Parts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="Orange"
                            Margin="2"
                            Padding="2"
                            CornerRadius="2">
                            <TextBlock Text="{Binding PartName}"
                                FontSize="12"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
