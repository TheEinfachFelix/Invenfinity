<UserControl x:Class="InvenfinityApp.Elements.LocationTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InvenfinityApp.Elements"
             mc:Ignorable="d" 
             xmlns:dto="clr-namespace:Backend.Application.DTOs;assembly=Backend"

             d:DesignHeight="450" d:DesignWidth="800">
    
        <TreeView Name="GroupView" Grid.Row="0" Grid.Column="0" ItemsSource="{Binding RootItems}" SelectedItemChanged="GroupView_SelectedItemChanged">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type dto:DTOTreeLocation}" ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Path=Name}" Background="LightGray"/>
                </HierarchicalDataTemplate>
                <DataTemplate DataType="{x:Type dto:DTOTreeGrid}" >
                    <TextBlock Text="{Binding Path=Name}" />
                </DataTemplate>
            </TreeView.Resources>
        </TreeView>
</UserControl>
